    @define(kw_only=True)
    class {{ attr.item.name|class_name}}:
        """
        {{ attr.item.longname }}
        {{ attr.item.description }}

        Parameters
        ----------
        {% for _, field in attr.item.fields.items() %}
        {{ macros.docstrings(field)|indent(8) }}
        {% endfor %}
        """
        {% for _, field in attr.item.fields.items() %}
        {{ field.name }}: {{ field | type_str }} = field({{ macros.metadata(field) }}{{ macros.default_in_field(field) }})
        {% endfor %}
