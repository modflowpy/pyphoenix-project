# autogenerated file, do not modify
{% import 'macros.py.jinja' as macros %}
{% set title = (name|title).title() %}
{% set description = name|description %}

from flopy4.mf6.model import Model
from xattree import xattree, field

def _convert_grid(value):
    if isinstance(value, Grid):
        return Dis.from_grid(value)
    if isinstance(value, Dis):
        return value
    raise TypeError(f"Expected Grid or Dis, got {type(value)}")

@xattree
class Modflow{{ title }}(Model):
    """
    {{ description }}

    Parameters
    ----------
    {{ macros.docstrings(dfn|variables)|indent(4) }}
    """

    {% for attr in dfn | attrs %}
    {{ attr.name | safe_name }}: {{ attr | python_type }} = {{ macros.xattree_field(attr) }}
    {% endfor %}
