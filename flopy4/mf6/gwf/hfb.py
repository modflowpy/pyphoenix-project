# autogenerated file, do not modify
# ruff: noqa: E501
from typing import Optional

import numpy as np
from attrs import Converter
from numpy.typing import NDArray
from xattree import xattree

from flopy4.mf6.converter import structure_array
from flopy4.mf6.package import Package
from flopy4.mf6.spec import array, dim, field


@xattree(kw_only=True)
class Hfb(Package):
    print_input: bool = field(
        block="options", default=False, longname="model print input to listing file"
    )
    maxhfb: Optional[int] = dim(
        block="dimensions", coord=False, default=None, longname="maximum number of barriers"
    )
    cellid1: Optional[NDArray[np.int64]] = array(
        block="period",
        dims=("nper", "ncelldim"),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="first cell adjacent to barrier",
    )
    cellid2: Optional[NDArray[np.int64]] = array(
        block="period",
        dims=("nper", "ncelldim"),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="second cell adjacent to barrier",
    )
    hydchr: Optional[NDArray[np.float64]] = array(
        block="period",
        dims=("nper",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="barrier hydraulic characteristic",
    )
