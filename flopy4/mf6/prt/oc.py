# autogenerated file, do not modify
# ruff: noqa: E501
from pathlib import Path
from typing import ClassVar, Optional

import attrs
import numpy as np
from attrs import Converter
from numpy.typing import NDArray
from xattree import xattree

from flopy4.mf6.converter import structure_array
from flopy4.mf6.package import Package
from flopy4.mf6.record import Record
from flopy4.mf6.spec import array, dim, field, path
from flopy4.utils import to_path


@xattree(kw_only=True)
class Oc(Package):
    @attrs.define
    class TrackTimes(Record):
        _keyword: ClassVar[str] = "track_times"
        times: float = attrs.field()

    @attrs.define
    class TrackTimesfile(Record):
        _keyword: ClassVar[str] = "track_timesfile"
        timesfile: str = attrs.field()

    budget_file: Optional[Path] = path(
        block="options", default=None, converter=to_path, inout="fileout"
    )
    budgetcsv_file: Optional[Path] = path(
        block="options", default=None, converter=to_path, inout="fileout"
    )
    track_file: Optional[Path] = path(
        block="options", default=None, converter=to_path, inout="fileout"
    )
    trackcsv_file: Optional[Path] = path(
        block="options", default=None, converter=to_path, inout="fileout"
    )
    track_release: bool = field(block="options", default=False, longname="track release")
    track_exit: bool = field(block="options", default=False, longname="track domain exits")
    track_subfeature_exit: bool = field(block="options", default=False, longname="track cell exits")
    track_timestep: bool = field(block="options", default=False, longname="track timestep ends")
    track_terminate: bool = field(block="options", default=False, longname="track termination")
    track_weaksink: bool = field(block="options", default=False, longname="track weaksink exits")
    track_usertime: bool = field(
        block="options", default=False, longname="track user-specified times"
    )
    track_dropped: bool = field(
        block="options", default=False, longname="track drops to water table"
    )
    track_times: Optional[TrackTimes] = field(block="options", default=None)
    track_timesfile: Optional[TrackTimesfile] = field(block="options", default=None)
    dev_dump_event_trace: bool = field(
        block="options", default=False, longname="print particle tracking events"
    )
    ntracktimes: Optional[int] = dim(
        block="dimensions", coord=False, default=None, longname="number of particle tracking times"
    )
    time: Optional[NDArray[np.float64]] = array(
        block="tracktimes",
        dims=("ntracktimes",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="release time",
    )
    save_budget: Optional[NDArray[np.str_]] = array(
        dtype=np.dtypes.StringDType(),
        block="period",
        dims=("nper",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
    )
    print_budget: Optional[NDArray[np.str_]] = array(
        dtype=np.dtypes.StringDType(),
        block="period",
        dims=("nper",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
    )
