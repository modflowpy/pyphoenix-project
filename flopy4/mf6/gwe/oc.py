# autogenerated file, do not modify
# ruff: noqa: E501
from pathlib import Path
from typing import ClassVar, Optional

import attrs
import numpy as np
from attrs import Converter
from numpy.typing import NDArray
from xattree import xattree

from flopy4.mf6.converter import structure_array
from flopy4.mf6.package import Package
from flopy4.mf6.record import Record
from flopy4.mf6.spec import field, keystring, path
from flopy4.utils import to_path


@xattree(kw_only=True)
class Oc(Package):
    @attrs.define
    class Temperatureprint(Record):
        _keyword: ClassVar[str] = "temperature"
        _extra_tokens: ClassVar[tuple[str, ...]] = ("PRINT_FORMAT",)

    budget_file: Optional[Path] = path(
        block="options", default=None, converter=to_path, inout="fileout"
    )
    budgetcsv_file: Optional[Path] = path(
        block="options", default=None, converter=to_path, inout="fileout"
    )
    temperature_file: Optional[Path] = path(
        block="options", default=None, converter=to_path, inout="fileout"
    )
    temperatureprint: Optional[Temperatureprint] = field(block="options", default=None)
    save_temperature: Optional[NDArray[np.str_]] = keystring(
        block="period",
        dims=("nper",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
    )
    save_budget: Optional[NDArray[np.str_]] = keystring(
        block="period",
        dims=("nper",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
    )
    print_temperature: Optional[NDArray[np.str_]] = keystring(
        block="period",
        dims=("nper",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
    )
    print_budget: Optional[NDArray[np.str_]] = keystring(
        block="period",
        dims=("nper",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
    )
