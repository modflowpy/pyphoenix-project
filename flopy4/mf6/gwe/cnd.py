# autogenerated file, do not modify
# ruff: noqa: E501
from typing import Optional

import numpy as np
from attrs import Converter
from numpy.typing import NDArray
from xattree import xattree

from flopy4.mf6.converter import structure_array
from flopy4.mf6.package import Package
from flopy4.mf6.spec import array, field


@xattree(kw_only=True)
class Cnd(Package):
    xt3d_off: bool = field(block="options", default=False, longname="deactivate xt3d")
    xt3d_rhs: bool = field(block="options", default=False, longname="xt3d on right-hand side")
    export_array_ascii: bool = field(
        block="options", default=False, longname="export array variables to layered ascii files."
    )
    export_array_netcdf: bool = field(
        block="options", default=False, longname="export array variables to netcdf output files."
    )
    alh: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        netcdf=True,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="longitudinal dispersivity in horizontal direction",
    )
    alv: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        netcdf=True,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="longitudinal dispersivity in vertical direction",
    )
    ath1: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        netcdf=True,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="transverse dispersivity in horizontal direction",
    )
    ath2: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        netcdf=True,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="transverse dispersivity in horizontal direction",
    )
    atv: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        netcdf=True,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="transverse dispersivity when flow is in vertical direction",
    )
    ktw: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        netcdf=True,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="thermal conductivity of the simulated fluid",
    )
    kts: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        netcdf=True,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="thermal conductivity of the aquifer material",
    )
