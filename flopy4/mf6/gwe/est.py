# autogenerated file, do not modify
# ruff: noqa: E501
from typing import Optional

import numpy as np
from attrs import Converter
from numpy.typing import NDArray
from xattree import xattree

from flopy4.mf6.converter import structure_array
from flopy4.mf6.package import Package
from flopy4.mf6.spec import array, field


@xattree(kw_only=True)
class Est(Package):
    save_flows: bool = field(
        block="options", default=False, longname="save calculated flows to budget file"
    )
    zero_order_decay_water: bool = field(
        block="options", default=False, longname="activate zero-order decay in aqueous phase"
    )
    zero_order_decay_solid: bool = field(
        block="options", default=False, longname="activate zero-order decay in solid phase"
    )
    density_water: Optional[float] = field(
        block="options", default=None, longname="density of water"
    )
    heat_capacity_water: Optional[float] = field(
        block="options", default=None, longname="heat capacity of water"
    )
    latent_heat_vaporization: Optional[float] = field(
        block="options", default=None, longname="latent heat of vaporization"
    )
    porosity: NDArray[np.float64] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="porosity",
    )
    decay_water: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="aqueous phase decay rate coefficient",
    )
    decay_solid: Optional[NDArray[np.float64]] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="solid phase decay rate coefficient",
    )
    heat_capacity_solid: NDArray[np.float64] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="heat capacity of the aquifer material",
    )
    density_solid: NDArray[np.float64] = array(
        block="griddata",
        dims=("nodes",),
        default=None,
        converter=Converter(structure_array, takes_self=True, takes_field=True),
        longname="density of aquifer material",
    )
